const LoaderBtn = ({ onClick }) => {
  return (
    <div className="Btn">
      <button className="LoaderBtn" onClick={onClick}>
        Load More
      </button>
    </div>
  );
};
export default LoaderBtn;
